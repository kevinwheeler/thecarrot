<form action="/approve-articles" method="post">
  <div class="container">
    <div class="row">
      {{#each articles}}
        <div class="col-xs-12 col-sm-6">
          <img src="{{this.imageURL}}" class="img-responsive">
          <input type="checkbox" name="article_ids[]" value="{{this._id}}" />
          <a href="{{this.articleURL}}">
            <h2 class="">{{this.headline}}</h2>
          </a>
          <h3 class="">{{this.subline}}</h3>
        </div>
      {{/each}}
    </div>
    <div style="text-align: center;">
      <button type="button" id="kmw-fetch-more" style="margin-top: 10px; margin-bottom: 30px;">Fetch more results</button>
      {{! THIS FORM IS DUPLICATED IN articleTemplate.hbs }}
      <select id="kmw-article-view-approval" name="approval_verdict">
        <option value="approved">Approve</option>
        <option value="denied">Deny</option>
      </select>
      <input type="submit">
    </div>
  </div>
</form>

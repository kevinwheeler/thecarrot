<div class="center-text">
  <div class="container-fluid">
    <div class="row kmw-field-description">
      If you want credit for your headline, we will cite you as the author, put a link to your page under your headline on our website,
      and tell people they should like/follow you. Also, your headline may look something like this when shared on Facebook:
    </div>
    <div class="row post-anon-image-container">
      <div class="col-xs-12 col-sm-6 col-sm-offset-3 col-md-4 col-md-offset-4">
        <img src="{{creditSrc}}" class="img-responsive">
      </div>
    </div>
  </div>
  {{#if loggedIn}}
    <label class="post-anon-checkbox-label" for="post-anon-checkbox">Post Anonymously?</label>
    <input id="post-anon-checkbox" name="post_anonymously" type="checkbox">

    <div class="post-anon-author-container">
      <label></label>
      Which page would you like to receive credit for this headline?
      <select id="post-anon-author-url" name="author_url">
        {{#each userPages}}
          <option value="{{this.link}}">{{this.name}}</option>
        {{/each}}
      </select>
    </div>
  {{else}}
    <input name="post_anonymously" type="hidden" value="true">
    You are not logged in, so you will automatically be posting anonymously. If you want credit for your headline, please
    <a class="post-anonymously-login kmw-hyperlink-styles" href="#">Log in</a>.
  {{/if}}
  <br><button type="button" id="kmw-credit-next" class="btn btn-primary upload-next-button">Done</button>
</div>
